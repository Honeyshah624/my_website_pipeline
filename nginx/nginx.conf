events {}

http {
    server {
        listen 80;
        root /usr/share/nginx/html;
        index index.html;

        location /test {
            try_files $uri $uri/ /index.html;
        }
    }
}